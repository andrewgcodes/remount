---
name: Bug report
about: Something behaves differently from what the docs or protocol say
labels: bug
---

## What happened

## What you expected

## How to reproduce

<!-- Commit or version, OS, backend (process/docker/gvisor/firecracker),
     runtime profile, the exact commands. `remount status --json`,
     `remount inspect WS --json` and `remount doctor --json` output help;
     redact anything private. Never paste a credential. -->

## Evidence

<!-- Events (`remount events --ws WS`), logs, or a failing test. -->
